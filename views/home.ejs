<% include partials/header %>
	<% if (!user) { %>
				<p>Welcome to IT Library! Please <a href="/login">log in</a>.</p>
			<% } else { %>
				<p>Hello, <%= user.user.name %>. View your <a href="/profile">profile</a>.</p>
			<% } %>
<% include partials/footer %>				
		